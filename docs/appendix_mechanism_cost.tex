% Appendix section: cost of cross-type eviction.
% Place after \appendix; reference from the main text as Appendix~\ref{app:cost}.
%
% NOTE on denominators. Both percentages here are against the 2028.90 s
% profiled run, and that run length is stated in the prose on purpose.
% Table 1's shift column is a different run (unprofiled) and a different
% window (post-shift, excluding the cold pass), so dividing these figures
% by its 1096 s gives 0.07% and 0.96% instead of 0.04% and 0.5%. Naming
% the denominator is what stops a reviewer making that division.
%
% Relocation count and share of run time are measured, from the profiled
% kv2exp run at the reported length (Nexp=220), post commit d6ba38d: 6866
% pages relocated, 746.0 ms of GPU copy time, over 2028.90 s of benchmark
% wall clock. The run-level relocation and expert-load latencies there
% (0.109 ms, 0.956 ms) corroborate the microbenchmark figures in the table
% below. The pre-change run gave the same 6866 pages and the same 0.04%,
% which is the check that d6ba38d altered only host-side cost.
%
% The 0.5% host-side figure: victim selection took 10.48 s of a 2028.90 s
% run (790.15 + 4.10 + 1234.65), summing the two top-level counters,
% select_super_block (76,644 calls at 102 us) and select_kv_victim (7,666
% at 346 us). Measured on the same build as the relocation figures above,
% post commit d6ba38d, which cut select_super_block from 181 us to 102 us
% (13.88 s to 7.82 s). Profiling is enabled, which inflates CPU time, so
% 0.5% is an upper bound.
%
% Scope: 0.5% covers victim selection only. The per-page tracking
% callbacks (_on_prefix_added / _on_prefix_removed / _on_kv_allocation and
% the PrefixLRU ops they drive) are not instrumented, which is why the
% sentence says "choosing what to evict" rather than claiming to cover all
% host-side cost. Bound for a rebuttal, not measured: those callbacks fire
% once per KV block lifecycle, order 1e4 here (220 requests x 336 tokens /
% 16, plus 2 x 16 prompts x ~3000 / 16), each O(1) on an intrusive list,
% so ~0.2 s even at a generous 20 us per call, about 0.01% of the run.
% Relocation's own rename() cost is already inside relocate_page.
%
% A "flat as the pool fills" clause was cut deliberately. Selection cost
% is indeed flat in KV occupancy now (microbench_pool.py sweeps it; it was
% 197 ms per miss at 95% occupancy before the victim-search fixes), but it
% is still linear in M: select_super_block walks all M super-blocks on
% almost every miss, since the free-page guard added in d6ba38d turns out
% never to fire on this workload. Claiming the favourable scaling property
% next to an unfavourable one invites the question that exposes it.

\appendix

\section{Cost of Cross-Type Eviction}
\label{app:cost}

Only one direction of cross-type eviction moves data. When KV reclaims an
expert's super-block, the pool drops the expert's mapping and the weights
are reloaded from the host when next needed. When an expert reclaims KV
space it needs a contiguous super-block, so the cached pages sitting in
that region are copied elsewhere to keep them. This appendix measures that
copy and how often it happens.

We time data movement with CUDA events on the pool's transfer stream, in
the configuration of Section~\ref{sec:evaluation} (one NVIDIA L4, expert
slot 12\,MiB, $F=96$ pages of 128\,KiB), over 500 repetitions. A KV block
is global, so relocating one page copies it in all 16 layer buffers.

\begin{table}[h]
\centering
\caption{Data movement in the unified pool.}
\label{tab:cost}
\small
\begin{tabular}{l r r r}
\toprule
\textbf{Operation} & \textbf{Mean} & \textbf{Bytes} & \textbf{Rate} \\
\midrule
Expert load (pinned host to pool) & 0.946\,ms & 12\,MiB & 12.4\,GiB/s \\
KV page relocation ($16\times128$\,KiB) & 0.113\,ms & 2\,MiB & 17.2\,GiB/s \\
\bottomrule
\end{tabular}
\end{table}

The expert load is bounded by PCIe, and any expert-offloading system pays
it. One such load costs as much as $8.4$ relocations, so keeping a warm
page is cheap next to the transfer that displaces it.

In aggregate the cost stays small even though the mechanism is busy. Over
the shift from the KV-heavy to the expert-heavy phase, the pool relocated
$6{,}866$ pages, $0.04\%$ of that $2{,}029$\,s profiled run. The host-side
cost of choosing what to evict is $0.5\%$ of the same run, an upper bound
since profiling was enabled. The reported speedup is end-to-end
wall-clock time measured with the mechanism running, so these costs are
inside that number rather than additional to it.
